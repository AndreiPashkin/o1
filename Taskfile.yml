# https://taskfile.dev
version: '3'

includes:
  o1_test: ./o1_test
tasks:
  test:
    env:
      RUST_BACKTRACE: full
    cmds:
      - cargo test --features _slow-tests
  lint:
    cmds:
      - pre-commit run --all
  generate:
    deps:
      - task: o1_test:generate
